const stateDefault = {

    credentials: null
}
const UserReducer = (state = stateDefault, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
export default UserReducer;
